import {Dimensions} from 'react-native';

const width = Dimensions.get('window').width;
const height = Dimensions.get('window').height;

export const dimen = {
  width,
  height,
};
export const BASE_URL = 'http://192.168.1.146:3000/';
